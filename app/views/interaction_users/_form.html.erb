<%= form_for(@interaction_user) do |f| %>
  <% if @interaction_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interaction_user.errors.count, "error") %> prohibited this interaction_user from being saved:</h2>

      <ul>
      <% @interaction_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<!-- show process buttons -->
<br>
        <div class="row">
          
          <% if @allow_processing == 0 %>
           
        <div class="row">
  <div class="col-xs-2"><%= link_to '(1) Add / Remove Products', neworder_path(:order_id => @order_id), class: "btn btn-primary btn-xs" %> 
  </div>
  <div class="col-xs-2"> 
  <%= link_to '(2) Change Address / Name', address_path(:order_id => @order_id), class: "btn btn-primary btn-xs" %>
  </div>
  <div class="col-xs-2">
  <%= link_to "(3) Add Up Sell's", upsell_path(:order_id => @order_id), class: "btn btn-info btn-xs" %>
   </div>
  <div class="col-xs-2"> 
  <%= link_to '(4) Add Payments', payment_path(:order_id => @order_id), class: "btn btn-info btn-xs" %></div>


   <div class="col-xs-2"> 
  <%= link_to 'Review Order', review_path(:order_id => @order_id), class: "btn btn-success btn-xs" %></div>

            
          <% end %>
         
        </div>

<br>
<!-- show process buttons  -->
<% headers = ['Orderdate', 'Paid date', 'Return date', 'Invoice N', 'City', 'State', 'Prod', 'Invoice Amount'] %>
<%= CSV.generate_line headers %>
	 <% @vpp.each do |c| %>
	 <%= CSV.generate_line([c.orderdate.strftime('%d-%b-%y'), c.paiddate, c.returndate, c.invoice, c.city, c.state, c.prod, c.invoiceamount]).html_safe.strip  %>
	<% end %>
<% headers = ['Shdate', 'Order No', 'Invoice', 'Distcode', 'Prod', 'Trantype', 'Quantity', 'Totamt'] %>
<%= CSV.generate_line headers %>
	 <% @newwlsdet.each do |c| %>
	 <%= CSV.generate_line([c.stock_date.strftime('%d-%b-%y'), c.shdate, c.orderno, c.invoice, c.distcode, c.prod, c.trantype, c.quantity, c.totamt]).html_safe.strip  %>
	<% end %>
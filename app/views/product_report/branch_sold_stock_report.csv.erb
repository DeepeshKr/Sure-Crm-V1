<% headers = ['Shdate', 'Orderno', 'Invoice', 'Distcode', 'Prod', 'Trantype', 'Quantity', 'Totamt'] %>
<%= CSV.generate_line headers %>
	 <% @tempinv_newwlsdet.each do |c| %>
	 <%= CSV.generate_line([c.shdate.strftime('%d-%b-%y'), c.orderno, c.invoice, c.distcode, c.prod, c.trantype, c.quantity, c.totamt]).html_safe.strip  %>
	<% end %>

	 	 	 	 	 	 	 	
<%= bootstrap_form_for(@order_master) do |f| %>
  <% if @order_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_master.errors.count, "error") %> prohibited this order_master from being saved:</h2>

      <ul>
      <% @order_master.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% unless @all_calllist.empty?  %>
	
  		<div class="row">
		<div class="col-xs-1">
			 <%= f.label :campaignplaylist_id, "Tv Ad"  %>
		</div>
 		<div class="col-xs-10">  
		    <%= f.select(:campaignplaylistid, 
		    options_from_collection_for_select(@all_calllist, :id, :playlistinfo), 
		    {}, :class => "form-control")  %>
		  </div>
  		</div> 		
  		
  		 


  		<div class="row">	 
  			
  			<div class="col-xs-1">
  			<label>Ads</label>
  			</div>
  			<div class="col-xs-11">
  				
  			
  			<div class="col-xs-2">
  				<label class="radio">	
		  				  				<%= label_tag(:mismatched_campaign, "All Ok") %>
		  				  		<%= radio_button_tag(:mismatched_campaign, "0", checked = true) %>
		  				</label>
  				</div>
  				<div class="col-xs-2">
  					<label class="radio">
  								<%= label_tag(:mismatched_campaign_1, "Ref Old ad") %>
						<%= radio_button_tag(:mismatched_campaign, "1") %>
						
						</label>
  					
  				</div>
  				<div class="col-xs-2">
  					<label class="radio">
					<%= label_tag(:mismatched_campaign_2, "Mismatch") %></td>
				<%= radio_button_tag(:mismatched_campaign, "2") %>
						</label>					
  			</div>
  			<div class="clearfix"></div>
  			<div id="customer_comment" class="col-xs-10">
  				 <%= f.text_area :comments,  :class =>"form-control" %>
  			</div>
  			</div>
  		</div>
  		
  <% end%>
	 <div class="row">
  <div class="col-xs-1">
  	 <label>Dialed </label>
  </div>
 
  <div class="col-xs-4">
  	  <%= f.text_field :calledno,  :class =>"form-control", :value => @calledno, :readonly => "true" %>
  </div>
  
 
  
</div>	
 
  
<% end %>

<div class="row">
<div class="col-xs-6">
<h3>Show Order for an employee</h3>
</div>
<div class="col-xs-6">
<div class="row">
<%= bootstrap_form_tag url: orderlist_path, :method => :GET, :enforce_utf8 => false do |f| %>
<div class="col-xs-8">
     <%= f.select(:employee_id, 
    options_from_collection_for_select(@employees, :id, :name, selected: @employee_id))  %>
    
  </div>
<div class="col-xs-3">
          <%= submit_tag "Show", name: nil,  class: "btn btn-primary" %>
      </div>
      <% end %>
</div>
</div>
</div>
<%= render('quicklist') %>
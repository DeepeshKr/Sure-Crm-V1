<h1>Listing states (Ensure the state list is VALID)</h1>
<div class="alert alert-warning">
<h4>Before Entering or modifying have to check the state in <%= link_to 'this list', india_pincode_lists_path, class: "btn btn-info btn-xs" %> </h4>
</div>

<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Name</th>
	  <th>Short Code</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @states.each do |state| %>
      <tr>
        <td><%= state.name %></td>
		<td><%= state.short_code %></td>
        <td><%= link_to 'Show', state %></td>
        <td><%= link_to 'Edit', edit_state_path(state) %></td>
        <td><%= link_to 'Destroy', state, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New State', new_state_path, class: 'btn btn-primary' %>

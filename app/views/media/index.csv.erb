<% headers = ['DNIS ', 'Channel Name', 'State', 'Telephone'] %>
<%= CSV.generate_line headers %>
	 <% @media.each do |c| %>
	 <%= CSV.generate_line([c.dnis, c.name, c.state, c.telephone]).html_safe.strip  %>
<% end %>

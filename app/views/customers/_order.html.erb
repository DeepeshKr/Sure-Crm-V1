<div class="row">
 
   <div class="col-xs-4">
  	  <%= f.text_field :calledno,  :class =>"form-control", :readonly => "true" %>
  </div>
  
 
  <div class="col-xs-4">
  	 <%= f.select(:media_id, 
		    options_from_collection_for_select(@medialist, :id, :ref_name), 
		      {:prompt => 'Please Choose one'}, :class => "form-control")  %>
  
  </div>
  <div class="col-xs-2">
  	
  </div>
</div>	
<%= f.fields_for :order_line do |builder| %>
    <%= render "order_line", :f => builder %>
  <% end %>
<% title "Edit Product Cost #{@product_cost_master.prod}" %>

<%= render 'form' %>
<%= link_to 'Destroy (if duplicate)', @product_cost_master, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger btn-sm" %> 

<%= form_for(@order_for) do |f| %>
  <% if @order_for.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_for.errors.count, "error") %> prohibited this order_for from being saved:</h2>

      <ul>
      <% @order_for.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

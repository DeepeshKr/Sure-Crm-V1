<%= bootstrap_form_for(@product_training_heading) do |f| %>
  <% if @product_training_heading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_training_heading.errors.count, "error") %> prohibited this product_training_heading from being saved:</h2>

      <ul>
      <% @product_training_heading.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   
    <%= f.text_field :name, :required => true %>
  </div>
  <div class="field">
   
    <%= f.number_field :sortorder, :required => true %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>

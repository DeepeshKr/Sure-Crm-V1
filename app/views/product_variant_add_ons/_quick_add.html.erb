<% if @product_variant_add_on.present? %>
<h3>Add Product Add-On to the product </h3>
<hr/>
<%= bootstrap_form_for(@product_variant_add_on) do |f| %>
  <% if @product_variant_add_on.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_variant_add_on.errors.count, "error") %> prohibited this product_variant_add_on from being saved:</h2>

      <ul>
      <% @product_variant_add_on.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  
    <%= f.hidden_field :product_master_id %>

   <div class="row">
    <div class="col-xs-10">
   <%= f.select :product_variant_id,  options_from_collection_for_select(@product_add_on_lists,
       :id, :description), { hide_label: :true}, { class: "selectpicker" }      %>
    
  </div>
 
  <div class="col-xs-1">
    <%= f.primary "Add", class: "btn btn-primary" %>
  </div>
  </div>
<% end %>

<% end %>
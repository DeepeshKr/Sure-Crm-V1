<%= form_for(@order_source) do |f| %>
  <% if @order_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_source.errors.count, "error") %> prohibited this order_source from being saved:</h2>

      <ul>
      <% @order_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

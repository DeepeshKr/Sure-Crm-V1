<%= bootstrap_form_for(@employee_role) do |f| %>
  <% if @employee_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_role.errors.count, "error") %> prohibited this employee_role from being saved:</h2>

      <ul>
      <% @employee_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
       <%= f.text_field :name, label: "Role Name - As close to business role " %>
  </div>
  <div class="field">
      <%= f.number_field :sortorder, label: "Level No (Lower level no means higher authority)", placeholder: "Lower level no means higher authority" %>
  </div>
  <div class="row">
   <div class="col-xs-2 col-offset-xs-8">
  
  <%= f.submit class: "btn btn-primary" %>
  
  </div>
  </div>  

<% end %>

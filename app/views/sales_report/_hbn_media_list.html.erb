
 <%= form_tag  url_for(:controller => 'sales_report', :action => searchaction), method: "get" do  %>

 <%= hidden_field_tag :from_date, @from_date %>
 <%= hidden_field_tag :to_date, @to_date %>
<div class="row">
  	<div class="col-xs-8">
   
    <%= select_tag :mediumid, options_from_collection_for_select(@hbnmedialist, :id, :mediainfo), {prompt: "Please select the HBN Media", label: "HBN Media", :class => "form-control"} %>
    
  	</div>
  	<div class="col-xs-4">
         <%= submit_tag("Show Report", :class => "btn btn-success btn-sm") %> 
	</div>
</div>
<% end %>
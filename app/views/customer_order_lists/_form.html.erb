<%= form_for(@customer_order_list) do |f| %>
  <% if @customer_order_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_order_list.errors.count, "error") %> prohibited this customer_order_list from being saved:</h2>

      <ul>
      <% @customer_order_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

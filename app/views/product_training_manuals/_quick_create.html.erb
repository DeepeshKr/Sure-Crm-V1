<%= bootstrap_form_for(@product_training_manual, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-10") do |f| %>
  <% if @product_training_manual.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_training_manual.errors.count, "error") %> prohibited this product_training_manual from being saved:</h2>

      <ul>
      <% @product_training_manual.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  	  
     <%= f.hidden_field :productid %>

  <div class="field">
  	 <%= f.select :name,  options_from_collection_for_select(ProductTrainingHeading.all,
       :name, :name),
    { label: "Heading"},
		       { class: "selectpicker" }
		        %>
  	
  </div>
  <div class="field">
   
    <%= f.text_area :description, label:"Description" %>
  </div>
  <div class="field">
   
    <%= f.text_area :quicknotes, label:"Quick Notes", placeholder:"Optional Quick Notes" %>
  </div>
  <div class="row">
  <div class="col-xs-2 col-md-offset-5">
    <%= f.primary class: "btn btn-primary" %>
  </div>
  </div>
<% end %>
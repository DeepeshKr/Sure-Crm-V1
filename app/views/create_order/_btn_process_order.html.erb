<div class="row">
		<div class="col-xs-4">
              
          <%= link_to "Add Products", showproducts_path(:order_id => @order_master.id), class: "btn btn-info" %>
          </div>

          <div class="col-xs-4 col-xs-offset-4">
          	<%= form_for @order_master , url: addpayment_path, :method => :POST do |f| %>
          		<%= hidden_field_tag :order_id, @order_master.id %>
           		<%= f.hidden_field(:id) %>

          		<%= submit_tag("Process Order", :class => "btn btn-primary") %> 
			<% end %>
        
          </div>
        
</div>
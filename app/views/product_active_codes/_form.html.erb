<%= form_for(@product_active_code) do |f| %>
  <% if @product_active_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_active_code.errors.count, "error") %> prohibited this product_active_code from being saved:</h2>

      <ul>
      <% @product_active_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sortorder %><br>
    <%= f.number_field :sortorder %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<% headers = ['Date','order_ref_id','order_no','manifest','tel_1','tel_2','cod_amount','pay_u_amount','payumoney_status'] %><%= CSV.generate_line headers %><% @pending_orders.each do |c| %><%= CSV.generate_line([(c.order_date.strftime("%Y-%m-%d")), c[:order_ref_id], c[:order_no], c[:manifest], c[:tel_1], c[:tel_2], c[:cod_amount], c[:pay_u_amount], c[:courier_name], (c.payumoney_status.name if c.payumoney_status)]).html_safe.strip  %> 
	<% end %>
	
